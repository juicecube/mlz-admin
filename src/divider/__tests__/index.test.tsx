import React, { Component } from 'react';
import { testMount, testSnapshot } from '../../../tests';
import Divider from '..';

describe('🧪 Divider', () => {
  testMount(Divider);
  testSnapshot(Divider);
});
