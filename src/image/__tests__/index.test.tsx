import React, { Component } from 'react';
import { testMount, testSnapshot } from '../../../tests';
import Image from '..';

describe('📦  Form', () => {
  testMount(Image);
  testSnapshot(Image);
});
