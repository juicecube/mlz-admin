import React, { Component } from 'react';
import { testMount, testSnapshot } from '../../../tests';
import Switch from '..';

describe('🧪 Switch', () => {
  testMount(Switch);
  testSnapshot(Switch);
});
