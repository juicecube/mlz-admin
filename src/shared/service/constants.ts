export type supporttingTypes = 'production' | 'development' | 'test' | 'staging' | 'dev';

export enum env {
  'production' = '',
  'development' = 'dev',
  'test' = 'test',
  'staging' = 'staging',
}
