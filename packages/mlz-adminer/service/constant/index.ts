export const isCompiled = !'$THIS_WILL_BE_EMPTY_AFTER_DIST$';

/**
 * Service System Root Host
 */
export const OpenServiceHOST = 'https://dev-open-service.codemao.cn';

/**
 * ⚠️ 不要把业务常量放进来，防止过多不相关代码
 * 进入各个业务组件。业务常亮请放在对应的业务逻
 * 辑中。 比如 api中的decodeEncodedPhone的DECODE_URL etc,
 */
