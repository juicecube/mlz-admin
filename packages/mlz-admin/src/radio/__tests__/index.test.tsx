import React, { Component } from 'react';
import { testMount, testSnapshot } from '../../../../../tests';
import Radio from '..';

describe('🧪 Radio', () => {
  testMount(Radio);
  testSnapshot(Radio);
});
