import React, { Component } from 'react';
import { testMount, testSnapshot } from '../../../../../tests';
import Badge from '..';

describe('🧪 Badge', () => {
  testMount(Badge);
  testSnapshot(Badge);
});
